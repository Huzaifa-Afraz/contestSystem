services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: contest_system
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 12345
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: